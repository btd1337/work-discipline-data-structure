class Sorter : Object {

/*  public static bubble_sort (Array<Review> collection) {
        bool isChanged;

        for (int i = 0; i < collection.length; i++) {
                for (int j = 0; j < collection.length - 1; j++) {
                        if (is_greater_than (collection.index (j), collection.index (j+1))) {
                                swap_items (collection, j, j+1);
                        }
                }
        }
   }
 */
/*  private bool is_greater_than (int val_a, int val_b) {
        return val_a.game_id > val_b.game_id ? true : false;
   }

   private void swap_items (Array<Review> collection, int index_a, int index_b) {
        Review aux_item = collection.index (j);
        collection.index (j) = collection.index (j+1);
        collection.index (j+1) = aux_item;
   }  */

}